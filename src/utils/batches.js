// Extend this list to add more batches — no schema migration needed since
// batch is stored as a plain string on the student record.
const BATCHES = ['6 AM - 10 AM', '10 AM - 2 PM', '2 PM - 6 PM', '6 PM - 10 PM'];

module.exports = { BATCHES };
